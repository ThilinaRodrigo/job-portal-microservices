apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-db
  namespace: job-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-db
  template:
    metadata:
      labels:
        app: employee-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: employee_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: employee-db
  namespace: job-portal
spec:
  selector:
    app: employee-db
  ports:
    - port: 5432